// See https://aka.ms/new-console-template for more information
Console.WriteLine("Hello, World!");
// This is a test for Copilot in the Module3-Lab repository.    
// It is designed to test the Copilot's ability to understand and generate code.
// Sort a list of integers
List<int> numbers = new List<int> { 5, 3, 8, 1, 2 };
numbers.Sort();
// Print the sorted list
foreach (var number in numbers)
{
    Console.WriteLine(number);
}
// This is a test for Copilot in the Module3-Lab repository.

// It is designed to test the Copilot's ability to understand and generate code.
// Sort a list of strings
List<string> names = new List<string> { "Alice", "Bob", "Charlie" };
names.Sort();
// Print the sorted list
foreach (var name in names)
{
    Console.WriteLine(name);
}
// This is a test for Copilot in the Module3-Lab repository.
// It is designed to test the Copilot's ability to understand and generate code.
// Calculate the sum of a list of integers
List<int> values = new List<int> { 1, 2, 3, 4, 5 };
int sum = 0;
foreach (var value in values)
{
    sum += value;
}
// Print the sum
Console.WriteLine($"Sum: {sum}");
// This is a test for Copilot in the Module3-Lab repository.
// It is designed to test the Copilot's ability to understand and generate code.
// Find the maximum value in a list of integers
List<int> scores = new List<int> { 10, 20, 30, 40, 50 };
int maxScore = scores[0];
foreach (var score in scores)
{
    if (score > maxScore)
    {
        maxScore = score;
    }
}
// Print the maximum score
Console.WriteLine($"Max Score: {maxScore}");
// This is a test for Copilot in the Module3-Lab repository.
// It is designed to test the Copilot's ability to understand and generate code.
// Reverse a list of strings
List<string> fruits = new List<string> { "Apple", "Banana", "Cherry" };
fruits.Reverse();